<!DOCTYPE html>
<%#include "blogapp.h" %>
<% tfetch(QVariantMap, blogapp); %>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= controller()->name() + ": " + controller()->activeAction() %></title>
</head>
<body>
<p style="color: red"><%=$ error %></p>
<p style="color: green"><%=$ notice %></p>

<h1>Editing Blogapp</h1>

<%== formTag(urla("save", blogapp["id"]), Tf::Post) %>
  <p>
    <label>ID<br /><input type="text" name="blogapp[id]" value="<%= blogapp["id"] %>" readonly="readonly" /></label>
  </p>
  <p>
    <label>Title<br /><input type="text" name="blogapp[title]" value="<%= blogapp["title"] %>" /></label>
  </p>
  <p>
    <label>Body<br /><input type="text" name="blogapp[body]" value="<%= blogapp["body"] %>" /></label>
  </p>
  <p>
    <input type="submit" value="Save" />
  </p>
</form>

<%== linkTo("Show", urla("show", blogapp["id"])) %> |
<%== linkTo("Back", urla("index")) %>
</body>
</html>
